# CMF Explorer Environment Configuration

# =============================================================================
# SUPABASE CONFIGURATION (Required for Production & Development)
# =============================================================================
# For multi-environment setup:
# - Use separate Supabase projects for dev and prod (recommended)
# - Or use one project with multiple redirect URLs (quick fix)
# See docs/MULTI_ENVIRONMENT_SETUP.md for detailed instructions

# Development: your-app-dev.supabase.co
# Production: your-app-prod.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# APP CONFIGURATION
# =============================================================================
# Development: http://localhost:3000
# Production: https://your-app.vercel.app or https://yourdomain.com
# CRITICAL: This must match the Site URL in Supabase Dashboard for password resets to work
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# DEVELOPMENT PERSISTENCE CONFIGURATION
# =============================================================================
# Controls how user data changes are saved in development mode
# Options:
#   - "database-first" (default): Save to database, fallback to files if database fails
#   - "file-only": Save only to local files for rapid iteration
NEXT_PUBLIC_DEV_PERSISTENCE_MODE=database-first

# =============================================================================
# DEVELOPMENT FILE SERVER (Optional)
# =============================================================================
# Enable automatic file updates in development
# Run: node dev-server.js
# DEV_FILE_SERVER_URL=http://localhost:3001

# =============================================================================
# USER PROFILE CREATION CONFIGURATION
# =============================================================================
# Controls fallback behavior for user profiles in development
# Options:
#   - "false" (default): New users get empty profiles, no fallback to companies.ts
#   - "true" (development): Use companies.ts as fallback for testing
NEXT_PUBLIC_USE_LOCAL_FALLBACK=false

# =============================================================================
# LOGO PROVIDER CONFIGURATION
# =============================================================================
# Logo.dev API key for company logo fetching
# Get your public key (starts with pk_) from https://logo.dev
# Note: This is a public key and safe to use client-side
NEXT_PUBLIC_LOGO_DEV_KEY=your-logo-dev-public-key

# =============================================================================
# LLM CONFIGURATION (Optional - for AI-powered company analysis)
# =============================================================================
# Anthropic API key for company analysis
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key

# =============================================================================
# E2E TEST CONFIGURATION (Optional - for automated testing)
# =============================================================================
# Credentials for Playwright e2e tests
# Create a test user in your Supabase project for automated testing
E2E_TEST_EMAIL=test@example.com
E2E_TEST_PASSWORD=testpassword123

# =============================================================================
# LANGFUSE LLM EVALUATION (Optional - for LLM eval and monitoring)
# =============================================================================
# Get your keys from https://cloud.langfuse.com (or self-hosted instance)
# Used to test prompt regressions and monitor LLM performance
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_HOST=https://cloud.langfuse.com